# Environment variables for Sintering Process Monitor

# --- Camera Credentials ---
CAMERA_USERNAME=camera_user
CAMERA_PASSWORD=camera_password
CAMERA_IP=
CAMERA_PORT=
# Specify the stream path required by the camera's RTSP URL structure
# Example for Mibo camera: /cam/realmonitor?channel=1&subtype=0
RTSP_STREAM_PATH=/cam/realmonitor?channel=1&subtype=0

# --- Video Settings ---
# Target width for frame processing/display (maintains aspect ratio)
TARGET_WIDTH=1280
# Target processing frames per second (limits resource usage)
# Uncomment and adjust as needed
TARGET_FPS=5

# --- Logging Settings ---
# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
# Path to the application log file
LOG_FILE=logs/your_file.log
